language: python

python:
  - "2.7"
  - "3.2"
  - "3.3"

before_install:
  - git submodule update --init --recursive

install:
  - sudo apt-get install cython python-dev
  - sudo pip install nose coverage coveralls --use-mirrors

script: 
  - ./travis-ci.sh